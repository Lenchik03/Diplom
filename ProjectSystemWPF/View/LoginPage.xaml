<Page x:Class="ProjectSystemWPF.View.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectSystemWPF.View"
      xmlns:vm="clr-namespace:ProjectSystemWPF.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#FFFDC401"
      Title="">
    <Page.Resources>
        <Storyboard x:Key="Lol">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="stackPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)">
                <EasingDoubleKeyFrame KeyTime="00:00:00" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="00:00:01" Value="360">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <BounceEase EasingMode="EaseIn"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>

    <Border BorderBrush="Black" BorderThickness="1" Margin="10">
    <Grid Background="LightYellow" >
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border BorderThickness="3" Grid.Row="1" Grid.Column="1" BorderBrush="#FFFDC401">
            <StackPanel x:Name="stackPanel"  Width="300" Height="220" Grid.Row="1" Grid.Column="1" RenderTransformOrigin="0.5,0.5">
                <StackPanel.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </StackPanel.RenderTransform>
                <Label Content="Авторизация" HorizontalAlignment="Center" FontSize="20"/>
                <Label Content="Введите эл.почту" FontSize="14" Margin="5"/>
                <TextBox  KeyDown="KeyDown" Margin="5" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding RelativeSource={RelativeSource Mode=Self}, Path=(Validation.Errors)[0].ErrorContent}" x:Name="loginBox"/>
                <Label Content="Введите пароль" FontSize="14" Margin="5"/>
                <PasswordBox KeyDown="KeyDown" x:Name="passwrdBox" ToolTip="{Binding RelativeSource={RelativeSource Mode=Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                <Button Content="Войти" Height="25" Width="80" Margin="10" FontWeight="Bold" Command="{Binding OpenPage}"
                        Click="MakeLol"
                />
            </StackPanel>
        </Border>
    </Grid>
    </Border>
</Page>
